# WasegMul — Deep-Dive Production Readiness Audit (Full Line-by-Line)

**Date:** 2026-09-18
**Auditor:** Muse Spark (4 parallel audit agents + direct verification)
**Scope:** every `.kt` (~90 files: `app/src/main`, `app/src/test`, `shared/commonMain`, `shared/commonTest`), every `.py` (8 scripts), every `.swift` (2 files), `AndroidManifest.xml`, 4× `build.gradle.kts`, `libs.versions.toml`, `gradle.properties`, `taxonomy.yaml`, `datasets_manifest.yaml`, `proguard-rules.pro`, `README.md`, `CONTEXT.md`, `docs/*`, assets.
**Baseline:** `CONTEXT.md` domain language (Category / Subclass / Waste Record / ML Arbitrator / YOLO Detector / Eco Impact / Knowledge Base / Barcode Product / Packaging Component / Barcode Resolution).
**Prior report:** `docs/DEEP_DIVE_AUDIT_REPORT.md` v1 (2026-09-18, Antigravity) — §12 below lists what is now FIXED vs still-open, to avoid regressions/misinformation.

Severity: **CRITICAL** = crash / data loss / security / native SIGSEGV · **HIGH** = wrong logic / broken feature / misinformation / data corruption · **MEDIUM** = UX / a11y / i18n / theming / perf · **LOW** = nit / drift.

---

## 1. Executive summary + scorecard

Ambitious architecture (dual EfficientNet + YOLO + 4-tier Barcode Resolution + KMP shared core + gamification + disposal GIS). Not yet studio-class: celebration/overlay resurrection bugs, Bengaluru-fallback location falsification, barcode XP/cache-poisoning bugs, ML preprocessing/normalization truth unresolved, taxonomy `Trash` vs `Residual`/`Hazardous` mismatch, and systemic light-mode / a11y / i18n gaps block a production release.

| Area | Score | Headline blockers |
|---|---|---|
| App UI / Navigation / UX | 5.5/10 | Dead Home recent-taps; trapped Classify screen; Result light-mode black text; ThankYou/LevelUp timed auto-dismiss a11y failures; Yolo snapshot-write off Main + inference on Main |
| Shared KMP domain logic | 6/10 | `subcategory` vs `Subclass` naming; `Trash` vs `Residual`, missing Hazardous branch; entropy lower-bound used as upper-bound; `arbitrateNeither` stale confidence; hardcoded English; no `@Serializable` |
| Data / ML / Infra | 5/10 | `fallbackToDestructiveMigration` wipe risk; unbounded export query; `BarcodeRepository` Main-thread + substring `contains("can")` misfires; rotation/quant/normalization unverified; `connectivity VALIDATED` missing |
| Scripts / Build / iOS / Tests / Docs | 5.5/10 | INT8 export w/o calibration; COCO-80 shipped as waste model; `normalize_ean13` checksum corruption; unpinned deps; iOS = 1 bridged call, no parity; `ModelBenchmarkTest` tests helpers not prod code; README numbers stale |
| Security / Privacy | 7/10 | No SQL injection; manifest + FileProvider + backup rules good. Gaps: unencrypted Room, `DailyImpactWorker retry-forever`, image retention, plaintext keystore on disk |
| Visual / Studio-class polish | 5/10 | `Color.Black`-on-primary, `Color.White`-on-glass, fixed green/yellow/red status colors recur; blur/particles ungated; donut clipping; mixed locale formatting; ~10sp text; <48dp targets |

**Release blockers (fix first):** §2 CRITICALs + §3 HIGHs + §11 roadmap Phase 1.

---

## 2. CRITICAL (crash / data loss / native crash / security-adjacent)

1. **Yolo snapshot-state written off Main thread** — `ui/yolo/YoloScreen.kt:253-256`. `ImageAnalysis` analyzer (background executor) assigns Compose `State` (`cameraFrameWidth/Height`) from worker thread → snapshot violation / crash. Fix: `runOnUiThread`/Main-handler or `MutableStateFlow` + `collectAsState`.
2. **Result black-on-primary + Barcode black-on-primary + YOLO capture black-on-primary** — `ui/result/ResultScreen.kt:518-523`, `ui/barcode/BarcodeScanScreen.kt:968-971,1310-1313`, `ui/yolo/YoloScreen.kt:354-357`. `Color.Black` on `colorScheme.primary` (light primary `0xFF1B8A4A`) → near-unreadable + WCAG fail. Fix: `onPrimary`.
3. **Disposal `ActivityNotFoundException` (no-maps devices)** — `ui/result/DisposalLocatorSection.kt:128-137`. Fallback generic `geo:` intent has no `try/catch`/`resolveActivity`. Fix: guard + chooser + snackbar. (Manifest `<queries>` now exists — verified — so this is a runtime guard gap, not a manifest gap.)
4. **Civic WhatsApp crash + `Unknown,Unknown` GPS link** — `ui/result/CivicReportingBanner.kt:103-110,104`. Hard-coded `https://wa.me/919448197197` unguarded; `lat/lon ?: "Unknown"` builds `?q=Unknown,Unknown` and sends it. Fix: `try/catch` + `resolveActivity`, hide/disable when location null, Remote-Config numbers.
5. **GamificationCard white-on-glass in light mode** — `ui/components/GamificationCard.kt:62,83,96,99`. `Color.White` + `0xFF00FF94` unreadable on light glass. Fix: `onSurface`/`onPrimaryContainer` tokens.
6. **Duplicate TFLite residency (OOM)** — `ui/classify/ClassificationViewModel.kt:62-66`. `initModel()` builds a second `ModelManager`; app + barcode VM + YOLO detector can all be resident. `onCleared:297` closes only this instance. Fix: shared singleton/session-scoped manager.
7. **Destructive migration wipes user data** — `data/WasteDatabase.kt:108` `fallbackToDestructiveMigration()` + only `MIGRATION_6_7…10_11`. Any v1–v5 install or future schema slip silently wipes `waste_history` + barcode cache. Fix: remove fallback (keep `…OnDowngrade` only), crash loudly in debug, add schema tests.
8. **YOLO close-vs-inference race (SIGSEGV)** — `ml/YoloDetector.kt:426-438` (`close` uses `detectMutex.tryLock`, init uses `initMutex`) + `ui/yolo/YoloScreen.kt:122-130` Main-thread `close()` vs background `detect()`. Fix: single shared mutex/state around init+detect+close.
9. **`keystore.properties` + `release-key.jks` plaintext on dev disk** — verified **NOT committed** (`.gitignore:35-36` correct, `git ls-files` empty, `app/build.gradle.kts:15-21,92-112` fail-closed). Still HIGH discipline risk: dictionary-composable password `WasegMulReleaseKey2026!`, root-located, README tells users to drop secrets in project root. Fix: env vars / CI secrets + rotation + `chmod 600` note. Instant CRITICAL if ever `git add -f`.
10. **Taxonomy `Hazardous`/`Residual` absent from runtime** — `shared/.../WasteMapping.kt:24-55`, `ml/classifiers/CategoryClassifier.kt:7`. Code uses `Trash`; CONTEXT.md mandates `Organic/Recyclable/Hazardous/E-Waste/Residual`. `EcoImpactCalculator:91-92,109-110` Hazardous branch near-dead. Fix: align CONTEXT or code + label assets (see §6).

---

## 3. HIGH — logic flaws / broken features / data corruption / misinformation

### Navigation / Home / Classify / Result
11. **Home recent-taps dead** — `navigation/AppNavigation.kt:57-80`. `Home` destination never passes `onNavigateToResult` (defaults `{}`) → most visible list does nothing. Fix: wire `Screen.Result.createRoute(record.id)`.
12. **Stale-record + spinner-forever nav** — `navigation/*`. Classify/Barcode→Result uses `launchSingleTop` with query args (may suppress new navigation, keep old record); History→Result omits it (duplicate pushes); `result` with no args (`recordId=-1`) = indeterminate spinner, no timeout/error (`ResultScreen.kt:530-545`). Fix: one policy + timeout/error state.
13. **User trapped during inference** — `ui/classify/ClassifyScreen.kt:76,205-208`. Back + Retake disabled while `isLoading`, no Cancel; system-back gesture still pops (inconsistent, half-written state, XP may already be awarded). Fix: cancellable `Job` + Cancel button + `BackHandler`.
14. **Dead-end Classify empty state** — `ClassifyScreen.kt` (`capturedBitmap==null` after `releaseBitmap`/process death) shows "Awaiting Visual Input" with no pick CTA. Fix: pick-image CTA + restore path.
15. **Result location falsified** — `ui/result/ResultScreen.kt:70-92` (verified). Permission checked but never requested; denial → hard-coded Bengaluru `12.9716,77.5946`, still titled "Nearest". Fix: request launcher + explicit "default location / enable location" disclosure + empty state (also `DisposalLocatorSection.kt:28` silent `return` on empty).
16. **History images never render** — `ResultScreen.kt:215,463`. `loadRecord` clears bitmap; body only shows `capturedBitmap`, never `record.imagePath` → "Image saved" text beside no image. Fix: Coil/`AsyncImage` from `imagePath`.
17. **Barcode body vs top-bar parse divergence** — `ResultScreen.kt:126-133` (source/barcode/vector) vs `:244` (vector only), `limit=2` vs `limit=3`. Fix: single `BarcodeDisplayModel` parser.
18. **History single-delete says "Delete All"** — `ui/history/HistoryScreen.kt:291-296,141`. Fix: `common_delete` string.
19. **Correction picker corrupts taxonomy** — `HistoryScreen.kt:460` (`R.array.material_options` mixes Categories `E-Waste/Organic/Trash` with Subclasses) → Category lands in `correctedSubclass`, `getCategory()` → `UNKNOWN`, category stays stale (`ClassificationViewModel.kt:227-238`). Also `FeedbackSection.kt` chips reuse same array; `initialCorrection` stale flash. Fix: subclass-only list, validate + reject Categories, rename param to `correctedSubclass`.
20. **False-empty flash** — `viewmodel/HomeViewModel.kt` (`stateIn(emptyList()/0)`) + `HistoryScreen.kt`. Initial value indistinguishable from loaded-empty → EmptyState flashes pre-Room. Fix: loading flag/nullable initial + skeleton vs CTA.
21. **Errors swallowed** — `HomeViewModel.kt` sets `_error` on clear/delete/feedback/correction; no screen collects it. `SettingsScreen.kt` purge failure silent. Fix: snackbar collection everywhere.
22. **Gallery pick silent drop; camera-permission loop; stale `tempPhotoUri`** — `ui/home/HomeScreen.kt:97-110,112,382-384`. Null decode → no feedback; permission launched every tap, no rationale/Settings path; process-death URI may be gone. Fix: snackbar, `checkSelfPermission` + rationale + Settings deep-link, validate file exists.
23. **Impact dialog grouping bug** — `HomeScreen.kt:596-606`. Case-sensitive unsorted `groupBy(category)`, unbounded list in dialog. Fix: normalize + sort + `LazyColumn`.
24. **`home_description` overclaim** — "Industrial material analysis … precise chemical confidence" for a visual classifier. Fix: "visual AI estimate".
25. **YOLO: inference on Main; permission asked blindly; dead resources; 16dp close target** — `YoloScreen.kt` (§17 prior). `scope.launch{ detectFrame() }` on Main + synchronous `d.detect()` → jank; `LaunchedEffect(Unit)` fires even when granted, no Settings redirect; `"DETECTED: N"`/`"+N more"` hard-coded while `yolo_detected_count`/`yolo_more` unused; dismiss `16.dp` vs 48dp min. Fix: `Dispatchers.Default`, permission check-first, use resources, 48dp target, `applyConfidenceThreshold` wiring, `cropRoi` normalized-vs-pixel contract check, Main-thread camera-unbind verification.

### Barcode pipeline
26. **Barcode scans earn no XP (wiring)** — `ui/barcode/BarcodeScanScreen.kt:159-165` + `AppNavigation.kt:157-164` omit `settingsManager`; `BarcodeScanViewModel.confirmAndSave:217` null-skips XP/streak block. Fix: inject + call.
27. **Manual entry dead-ends from error states** — `BarcodeScanScreen.kt:240-258` + VM early-return unless `Scanning`. Dialog also has zero validation messaging, hard-coded English. Fix: allow from `NotFound`/`Error`, validate + message.
28. **Bottom sheet no-cancel; Save deadlock** — `BarcodeScanScreen.kt:348,854-356`. `Resolving` spinner un-cancellable (OFF cascade up to ~60s); `isSaving` never resets, suspend `confirmAndSave` uncaught → permanently disabled button, no error UI. Fix: Cancel/resume, `try/catch` + reset + snackbar, `trySend` not `send` (`BarcodeScanViewModel.kt:236` hangs if popped).
29. **Visual-ML cache poisoning + fake barcodes cached + fabricated weights** — `ui/barcode/BarcodeScanViewModel.kt:153-155,249-282,257-261,192`. Tier-4 guess persisted as ground truth (`source="visual_ml"`); `"code_${timestamp}"` junk keys in Room; heuristics (E-Waste 1500g, Metal/Glass 250g, else 50g) fabricate CO₂/water/energy. Fix: never persist guesses as ground truth (mark `unverified`, TTL, re-verify), never cache `code_*`, weigh-or-null + disclose estimate.
30. **Threshold inconsistency** — Tier-4 `MLArbitrator.arbitrate(...)` ignores user `confidenceThreshold` that camera honors. Fix: pass threshold through.
31. **Legacy pipe-tag triple-storage** — `featureVector="barcode:code|name|brand"` alongside `source/productName/barcode` → every consumer re-parses differently (`limit=2` vs `3`, `|` in names breaks). Fix: single parser + migrate off vector; word-boundary keyword matching (below).

### Data / ML / Repos
32. **`BarcodeRepository.resolve()` Main-thread + no sanitize + substring misfires + 60s fan-out** — `repository/BarcodeRepository.kt:128,131,207-232` + `network/OpenFoodFactsApi.kt:118-139`. DAO I/O + 6 sequential 10s calls on caller dispatcher (ANR); unsanitized input (spaces/dashes/URLs from `FORMAT_ALL_FORMATS`) hits DAO/network; `contains("can")` fires on candy/canola/American, `"box"` on Xbox, `"bar"` on barcode; no parallelism/deadline/backoff; `CancellationException` swallowed (`OpenFoodFactsApi.kt:135-137`, rotation leaks); triple API-surface conventions (`getProduct` vs `queryEndpoint` vs `getProductCascade`); `isPreloadChecked` racy; `context==null` silently disables Tier 1; seed leak (`sqliteDb.close`/`tempFile.delete` skipped on throw); `IGNORE` drops taxonomy updates; no negative cache; `Trash` default + optimistic `Recyclable/Cardboard`; `pruneCache` deletes `user_identified`; `"api_cache"` vs `"api"` vocab split. Fix: `withContext(IO)`, trim/validate (EAN-8→GTIN-14, checksum), word-boundary regex, `async` fan-out + `withTimeout(15s)` + backoff, rethrow Cancellation, unify null-conventions, `@Volatile`+mutex, `try/finally`, versioned upsert, miss-marker TTL, `Residual`, preserve user entries, unify vocab.
33. **`ImagePreprocessor` normalization truth unresolved (accuracy blocker)** — `ml/preprocessing/ImagePreprocessor.kt:23-25,40-42` feeds raw `[0,255]` FLOAT32, no `NormalizeOp`; `README.md:182,192` claims `0.0–1.0`; training config unverified. If EfficientNet trained with rescale/`preprocess_input`, accuracy collapses. Also: no rotation/EXIF (portrait misclassified), HARDWARE-bitmap null-crash (`copy()` nullable platform type → NPE at `centerCropToSquare`), doc mismatch. Fix: confirm training preprocessing, add matching `NormalizeOp` or fix README + regression test asserting output range; rotate via EXIF/`rotationDegrees` before crop; null-check.
34. **YOLO quantized/sigmoid/coordinate heuristics + silent failure** — `ml/YoloDetector.kt:101,200,216-218,330-343`. Non-float branch ignores zero-point/scale; sigmoid applied post-argmax on winner only; `cx<=1` guesses normalized-vs-pixel (small near-origin pixel boxes mis-scaled ×640); `catch → emptyList()` hides inference crashes. Also duplicate model candidate, silent COCO fallback, fixed 4 threads, per-class-only NMS. Fix: scale/zero-point, pre-argmax logit handling, explicit normalized flag from model meta, `Result` return, dedup candidates, device-aware threads, cross-class NMS.
35. **`TfliteClassifier` quant/shape/softmax gaps + `Trash` label asset** — `ml/classifiers/TfliteClassifier.kt:39-46,64-66` + `CategoryClassifier.kt:7`. UINT8 via `mapWithFloatValue` unscaled; shape check only `last()==labels.size` (no batch/rank/4-vs-15 guard → swapped model mislabels); logits returned as calibrated confidence; `@WorkerThread` undocumented. If `category_classes.txt` contains `Trash`, rename to `Residual` + remap. Fix: dtype-aware dequant, strict shape assert, calibrate/document, suspend factory.
36. **`BarcodeScanner` rotation fallback + no timeout + weak GTIN regex** — `ml/BarcodeScanner.kt:89-101,169-191`. `InputImage` rotation correct but `toBitmap()` fallback ignores `rotationDegrees` (sideways Tier-4); `await()` no timeout + `imageProxy` held open → pipeline stall; regex misses `?gtin=`, slash-less `/01…`, whitespace QR, EAN-8. (Checksum weights verified correct.) Fix: rotate fallback bitmap, `withTimeout`, broader parser + junk rejection pre-OFF.
37. **Room entity bypasses validation; mappers crash Flow** — `data/WasteRecord.kt:22-38` accepts NaN/<0/>1/negative weight (shared `WasteRecord` has `require()` guards) → `Mappers.toCommon():10-25` throws at read time, crashing collectors (`WasteRepository.allHistory`); `DailyImpactWorker.kt:46-62` hand-rolls same mapping (drift). Fix: validate/coerce at write + `toCommon`, reuse mapper, CHECK constraints, enum `source`/`feedback`.
38. **DAO unbounded loads; fake pagination** — `data/WasteDao.kt:53-54,12-16,44,47-48`. `getAllForExport()` no `LIMIT` (OOM) + `ORDER BY timestamp`; `LIMIT 500` no `OFFSET`/keyset; `getRecordsSince()` full entities where counts/SUMs suffice. Fix: paginate/keyset/Paging3, `COUNT`/`SUM` queries. (Also `updateCorrectionFull` atomic — OK; SQL injection safe — bound params — OK.)
39. **Barcode cache normalization + DAO races + over-prune** — `data/BarcodeProduct.kt:21`, `data/BarcodeProductDao.kt:14-35`. `0123…` vs `123…` vs padded dup rows; `find+updateLastAccessed` non-transactional; unbounded `DELETE`; `source != 'preloaded'` deletes `user_identified`; `REPLACE` resets `cachedAt`. Fix: normalize at insert, `@Transaction`, `LIMIT` loop, preserve user entries / longer TTL, upsert preserving `cachedAt`.
40. **`DisposalDatabase` ANR + leak + one-bad-row-kills-all + exact-match filter** — `data/disposal/DisposalDatabase.kt:15-76`. `assets.open` + `JSONArray` parse on caller thread, no cache; `InputStream` never closed; `getString/getDouble/getJSONArray` throw → partial/empty; `printStackTrace` hides corrupt-vs-empty; case-sensitive `in` with no normalization/fallback; only `Recyclable`/`E-Waste` civic contacts; Haversine `a>1` NaN edge. Fix: `suspend` + `Dispatchers.IO` + cache + `use{}`, per-item `runCatching`, `Result`/tagged log, normalize + fallback, add Hazardous/Organic contacts, clamp `a`.
41. **`SettingsManager`/YOLO threshold split; `LocalDate` desugar/testability; negative XP** — `utils/SettingsManager.kt:53,84,96`. Default `0.50f` vs `YoloDetector 0.45f`; `java.time` needs desugar check; no `Clock` injection; `setTotalXp` accepts negatives. Fix: single const/DataStore-backed detector, verify desugar, inject clock, clamp ≥0.
42. **`ConnectivityChecker` captive-portal false-online; 8PM drift; no pre-warm** — `utils/ConnectivityChecker.kt:11-17`, `WasegMulApp.kt:50-61`. `NET_CAPABILITY_INTERNET` ≠ validated uplink → Tier-3 delayed fail; `PeriodicWorkRequest(1 DAY)` + initial-delay drifts under Doze, `KEEP` never re-anchors; first classification pays mmap jank. Fix: `NET_CAPABILITY_VALIDATED`, `AlarmManager`/`REPLACE` + backoff + constraints, background pre-warm.
43. **CameraX backpressure defeated; bitmap recycle chain** — `YoloScreen.kt:472-480` releases guard before `onFrameCaptured` (overlap defeats `KEEP_ONLY_LATEST`); `copy(ARGB_8888)` must be recycled downstream; `BarcodeScanScreen.kt:498-509` holds proxy across no-timeout `await()`. Fix: release after dispatch / drop counter, verify recycle, executor shutdown in `DisposableEffect`.
44. **`GamificationManager` farms XP; taxonomy drift** — `gamification/GamificationManager.kt:52-66`. `coerceAtLeast(5)` rewards Unknown/0-conf/dupes (spam-leveling); flat `+10` streak; negative CO₂ floored then clamped; `"Trash"->1.0`, no Hazardous/Residual. (Per-scan cap now exists per prior fix — keep + verify `coerceAtMost(150)`.) Fix: min-confidence + dedup window, align multipliers to CONTEXT, clamp input ≥0.
45. **`DailyImpactWorker` retry-forever + no timeout** — `notification/DailyImpactWorker.kt:67,90`. `Result.retry()` on all exceptions (DB corruption/DataStore proto → battery drain); `totalXp.first()` no timeout (10-min stall). Fix: retry only `IOException`/`HttpException`, `withTimeout(10_000)`, reuse `Mappers.toCommon()`, `LocalDate` DST note.
46. **`PredictionCodec.decode` crash + range + legacy-Unicode gaps** — `shared/.../PredictionCodec.kt:3,30-86`. `%FF%FE` → `decodeToString()` throws on Room read path; `5.0`/`-1.0` pass `isFinite` then violate `0..1`; legacy raw Unicode truncated via `s[i].code.toByte()`; whitespace round-trip lossy; no size cap; unused import. Fix: `runCatching`/replacement decoding, `0..1` clamp/reject, explicit charset, caps.
47. **Scripts: INT8 w/o calibration; COCO shipped as waste; checksum corruption; taxonomy forks** — `scripts/kaggle_train.py:960`, `export_waste_model_tflite.py:59,114-115`, `export_yolo_tflite.py:27,43-47`, `build_barcode_db.py:62-200,290-319,1030,1040`, `kaggle_train.py:102-170` vs `taxonomy.yaml:77-306,187-188`, `train_waste_yolo.py:122-143`. No calibration dataset (throws or accuracy collapse — never ship `*_int8` without `data=` + AP regression); stock COCO-80 exported to assets while docstring implies drop-in waste model (runtime guard exists but script misleads; COCO-0 person → waste-0 battery without guard); `normalize_ean13` pads/truncates invalidating GS1 Modulo-10 (checker itself correct) + 12-digit UPC-A stored raw → 13-digit misses; fallback taxonomy dict omits blister/foam/straws/rings + `Other plastic` included-vs-excluded per entry point; train auto-export omits labels file → stale/missing labels → COCO fallback; triple OFF-mapping truth (Python/Kotlin/yaml) drifted (`en:label` test-only). Also unpinned deps (all scripts), MD5-only dedup (burst-frame leakage → pHash + filename-group), `n<=2` classes get zero val split (warn-only verify), `cache=True` + `batch=-1` OOM on T4, 300ep/patience-50 exceeds Kaggle quota, hardcoded Kaggle paths/identities, `generate_taxonomy_md.py:90,107` hardcoded 15/30. Fix: §11 Phase 1 order.
48. **iOS = 1 bridged call, zero parity** — `iosApp/iosApp/ContentView.swift:1-89` does `import shared` + `IOSBridge.getWasteKnowledge` (real, not stub) but nothing else: static splash + `print()` scanner, no AVFoundation/CoreML-TFLite/barcode/history/EcoImpact/settings/nav; hardcoded dark/emerald dup `Color.kt`; `InfoRow` "16 KB Page Aligned" unverified; no pinned `:shared` version. Fix: AVFoundation + TFLite C-API/CoreML bridge, feature parity roadmap, theme tokens.
49. **Tests: helpers tested, prod code untested; matrix stale** — `app/src/test/.../ModelBenchmarkTest.kt:57-154` reimplements `calculateIoU`/`calculateShannonEntropy`, never touches `YoloDetector.iou`/`MLArbitrator.computeEntropy` (NMS/letterbox regressions pass green); zero tests for YOLO parse/NMS/letterbox, `ImagePreprocessor` crop/HARDWARE path, `TfliteClassifier` mismatch guard, `MIGRATION_8_9`, VMs, `SettingsManager`, CSV, worker, OFF errors; `BarcodeScannerTest.kt:19` locks `"0000000000000"` as valid (checksum passes, GS1-reserved — questionable fixture); `EcoImpactTest.kt:128` KB assertion misplaced; README `209-223` "8 suites + ExampleUnitTest" vs actual 13+3 (stale); shared `commonTest` missing Arbitrator/EcoImpact/Message/Codec/Mapping/KB/Bridge suites (only barcode/mapper/decay exist). Fix: `@VisibleForTesting` prod helpers / move math to shared + test it, fill seam tests, update matrix.

### Shared-logic specifics
50. **MLArbitrator: stale-confidence + tunable-threshold contradiction + entropy misuse + NaN + magic gates + taxonomy passthrough** — `shared/.../MLArbitrator.kt:32,38,44-49,175-182,240,260,291-296,321-323,364-398,417,467-483`. `arbitrateNeither` leaks stale confidences (all other UNCERTAIN paths zero); doc "subclass bar tracks low" vs fixed `0.65f:296` (low=0.95 still treats 0.70 as highly confident); `computeEntropy(empty)=0` = "certain" (should be max-uncertain); entropy lumps unaccounted mass into "rest" → documented lower bound used as upper bound (confused looks certain), `CEILING 1.2` unjustified for 4-class (max 2.0) and breaks on ≠4 lists; `NaN` survives `coerceIn`, defeats every `< NaN` + entropy check; `BOTH_AGREE_HIGH 0.80` magic; agree-check uses single `subcategory` vs cross-check uses `topSubcategories` (verdict depends on populated field); inconsistent zeroing destroys analytics; `mergeTopList` unnormalized (>100%); `arbitrateCategoryOnly` passes `"FooBar"` @0.99 through; `UNKNOWN` vs `UNCERTAIN` fragile (`getCategory` never returns `UNCERTAIN` → dead checks `:161,296`); barcode path blind-trusts `BarcodeEvidence` (0.95–0.99 magics, no taxonomy validation, conflict discards visual signal). Fix: zero/preserve policy per contract, track threshold, entropy caveats + guard empty/NaN + justify ceiling, validate taxonomy, constants + KDoc, preserve disagreement signal.
51. **EcoImpact: coarse factors + dead Hazardous + inflated counts + typo-drop + misnamed weight** — `EcoImpactCalculator.kt:22-41,63-135` (factors now match `architecture.md` — verified — but still coarse: Recyclable `4.2 kWh/kg` over-credits glass ~0.1 vs aluminum ~13; E-waste CO₂ `3.5` low vs phone LCA 10+; Hazardous appears invented — cite per-material sources, don't present as EPA-precise). `totalItems = records.size` counts excluded (zero-weight/incorrect/UNCERTAIN) while weight excludes them → "diverted" inflated; `correctedSubclass` typo → `UNKNOWN` → silently dropped (fall back or surface); `totalWeightKg` includes plain Trash (reads "diverted" but includes landfill — rename); accuracy counts any non-`"correct"` as incorrect; Recyclable/Organic hazardous credited at base rates; `List<WasteRecord>` iOS-hostile (flat overloads needed). Fix: citations + ranges, count only credited, fallback/surface, rename, enums, bridge overloads.
52. **MessageGenerator: low-confidence "highly dependable" + global RNG + duplicated match logic + lost context** — `shared/.../MessageGenerator.kt:21-29,35-56,273-299,335-345`. `BOTH_AGREE`/`BOTH_AGREE_HIGH` share pool → 0.55/0.55 emits "High-confidence … highly dependable"; `var rng` racy (prefer `random` param); ignores confidences except re-derivation (duplicates Arbitrator); `SUBCLASS_ONLY` re-derives category (breaks `IOSBridge` empty-tops path); `BARCODE_*` synthesizes `"Unknown Product"`; hardcoded English; `humanizeLabel` mangles `Air-Conditioner`/PET, locale-naive; `pct(NaN)` → 0%; `UNKNOWN` "best" match garbage; `list.random()` unguarded. Fix: split pools by confidence, inject random, single match source, preserve caller category, require product name, i18n plan.
53. **WasteMapping: key chaos + hazard misflags + wrong recyclability + absurd weights** — `WasteMapping.kt:25-92`. `Title-Case`/`lowercase spaces`/`snake_case` mixed (saved by case-insensitive map, but KB `when` is case-sensitive — §54); `Mobile/Laptop/Player/Electronic Device` not hazardous despite Li-ion (UI hazard-gate misses phones/laptops), `Printer=true` over-flags, `Washing Machine=false` vs `Microwave=true` inconsistent; `plastic_bag → Recyclable` (film jams curbside — store-drop-off only), `wine glass → Recyclable` (drinkware contaminates melt — KB itself bins Pyrex/ceramics/mirrors), `textile → Trash` vs `clothing → Recyclable` opposite credit for identical fibers; `automobile wastes 5.0kg` absurd (tire ~10kg, car ~1000kg), `Plastic 0.045` overstates bags 9×, `clothing 0.3` vs `Textile 0.1` unjustified; silent `UNKNOWN/0.05/false` defaults (dual `0.05` with `CommonModels:16`) no logging. Fix: normalize keys, hazard audit vs battery/fire risk, curbside-vs-store truth, per-material weights + provenance KDoc, log defaults.
54. **KnowledgeBase: case bug + missing Hazardous/Residual + contradictions + inverted stat + overgeneralizations** — `WasteKnowledgeBase.kt:23-28,46,91,108-115,135-142,155,261-301`. Canonicalize searches only `MAPPING` (not `EXTENDED`) then exact `when` → `getCategory("Bottle")` works but `getInfo("Recyclable","Bottle")` falls to generic `else` (every lowercase EXTENDED label affected when model capitalizes); no Hazardous/Residual guidance (`getCategoryLevelInfo("Hazardous")` → null); `Plastic`=bin-recyclable contradicts `PackagingWasteMapper` PVC/PS → `Trash/Plastic`; `wine glass` Recyclable vs drinkware rule; `Metal` "Mining ore is up to 95% more energy-intensive" inverts standard stat (recycling Al saves ~95% → mining ~20× — understates ~10×); TV "flat-screens contain mercury" (CCFL-only, not LED/OLED); phone/laptop omits Li-ion fire handling (cf. Battery tape/drop-off); `else` interpolates raw untrimmed subclass + self-cites "agrelius Sustainability Research"; bullets hardcoded English. Fix: canonicalize both maps + case-insensitive `when`/lookup, add Hazardous/Residual entries, reconcile Plastic/wine-glass, correct stat with citation, add Li-ion safety, trim + cite real sources.
55. **DecayTime: missing items + wrong buckets + fragile inputs + overgeneral singles + uncited** — `DecayTimeData.kt:20-177`. `cigarette` absent → `Trash` 50–1000y fallback overstates 5–100× (true ~10–12y); all EXTENDED shapes fall back vaguely (`plastic_bag` → 50–500y vs true 10–20y); `getComparison` buckets wrong (`<1.0` → "Less than a single season" — 0.9y ≈ 3 seasons; `<50` → "Longer than a generation" — 2y is not; re-anchor `0.25/25` + cite); no `.trim()` (vs Mapping); `Plastic 100–1000` ignores bags, `Metal 100–500` overstates cans, `Battery 100` understates indefinite metals, `automobile 200–2000` unsourced; `severityLevel 0–3` undefined (Glass 1M-y sev-1 vs E-waste sev-3 conflates persistence/toxicity); no Hazardous/Residual/Uncertain/Unknown fallbacks; year-animation misinformation (`DecayTimeSection.kt:115-121` flashes `"0 YEARS"` for sub-year items, truncates); all numbers uncited. Fix: fill gaps, fix buckets, trim, per-material ranges + citations, KDoc scale, animation uses `displayText` throughout.
56. **PackagingWasteMapper: `fr:` bug + positional pairing + overstated recyclability + order bug + weight passthrough** — `PackagingWasteMapper.kt:67-341`. Non-English lookup tries bare `"pet-1"` but keys all carry `"en:"` → `fr:pet-1` null (try `"en:"+withoutEn`); `materialsTags[i] ↔ shapesTags[i]` assumes OFF alignment (not guaranteed) + smears one recycling tag across components; `Tetra Pak → Recyclable/Cardboard` (needs specialty mills), `bag → Recyclable/Plastic` (store-only), `bioplastic/PLA → Organic` (industrial compost only — contaminates organics), PVC/PS → `Trash/Plastic` collides with KB Plastic=Recyclable; `mapDisposalAction:245-248` checks `"recycle"` before `"discard"` (`"do-not-recycle"` → Recycle; `"bin"` over-matches `combine`); shape fallback mislabels glass bottles as Plastic, aerosol omits pressurized/hazard handling, barrel/drum ignore residue; `lc_name` mixes locales; no weight sanity (g-vs-kg 1000×); synthesized path `weightGrams=null` makes heaviest-pick arbitrary; free-string `disposalAction`/`category`; `Cotton → Trash/Textile` vs `clothing → Recyclable`. Fix: prefix retry, independent component resolution (no positional pairing), specialty-stream truth, order `"discard"`-negations first + word boundaries, hazard-aware fallbacks, unit checks + explicit g→kg, enums.
57. **IOSBridge: silent mode-fallback + dropped evidence + truncation + no docs/parity** — `IOSBridge.kt:12-77`. `valueOf(mode)` failure → `BOTH_UNCERTAIN` ("typo → cannot identify"); `emptyList()` tops drops entropy/agreement; no barcode `productName` (can't emit barcode messages); `zip`/`minOf` silent truncation (require equal sizes); `decodePredictions` returns `"label|conf"` strings (Swift re-parse, decimal-comma fragility — return arrays/dicts); no `lowThreshold` param (Android slider parity); `List<WasteRecord>` Obj-C-awkward; `calculateImpactTotalWeight` duplicates `.totalWeightKg`; zero KDoc. Fix: strict mode parsing, preserve evidence, equal-size require, Swift-friendly types, threshold param, KDoc.
58. **OFF API/client: cancellation + conventions + leniency + path + ownership + validation** — `network/OpenFoodFactsApi.kt:32-153`, `OffModels.kt:24-53`. `catch (Exception)` swallows `CancellationException`; 3 null-conventions (`getProduct` magic `"product not found"`, `queryEndpoint` null=404, cascade null=miss; `200+null` vs `404` differ); lenient JSON w/o `coerceInputValues` (crowd bool/string `packagings_complete`/`number_of_units` throws); no 429/5xx backoff, error body discarded, no logging/retry/cache; v2 `.json?fields=` vs v3 query-param style unverified (integration test needed); `FIELDS` omits `product_name_en/lang`; `getProduct` FOOD-only vs cascade FOOD+BEAUTY undocumented; `close()` kills injected shared client; hand-rolled percent-encoding, no GTIN length/checksum validation (EAN-8/GTIN-14); `OffModels` grams undocumented (kg-assumer → 1000× impact error), `Int?` assumes numeric JSON, `lc_name` locale leakage, dead `ecoscoreGrade`. Fix: rethrow Cancellation + overall timeout, one convention, `coerceInputValues` + backoff + logging, verify path, document scope, don't close injected, validate GTIN, KDoc units, tolerant types.
59. **CommonModels: validation gaps + vocab + serialization + fail-open defaults** — `CommonModels.kt:16-95`. Only `WasteRecord` enforces `0..1`; `PredictionResult`/`ClassificationResult`/`InternalResult` accept `-5/2.0`; `source`/`feedback` free strings (typo forks analytics); `topPredictions: String?` no encode helper (must go via `PredictionCodec`); `timestamp 0L` persistable (1970); primary API says `subcategory` (forbidden — must be `subclass`); nothing `@Serializable` (`Pair` lists Obj-C-hostile → bridge hacks); `BarcodeEvidence.isComplete=true` fail-open (forgets → 0.98 ground truth on partials — default `false`); `productName` non-null forces `"Unknown Product"` invention; dual `0.05` defaults; missing KDoc (units/ranges/vocabs/vector format). Fix: one confidence policy + NaN guards, enums, helpers, sentinel rejection, rename, serialization, fail-closed defaults.

---

## 4. MEDIUM — usability / a11y / i18n / theming / perf (studio-class gaps)

- **No `BackHandler` anywhere** — sheets/dialogs/loading guards can't intercept system gesture. Add per-screen handlers.
- **Zero `deepLink{}` / intent-filters** for result/history/civic flows. Add when civic/share requires it.
- **Rotation / process-death losses** — bitmaps, filters (`searchQuery/selectedCategory/sortOrder`), form inputs (`manualBarcode/productName/selectedIndex`), overlay phases (`phase/visible`), dialog flags all bare `remember`; rotation replays entrances (Home 4×150ms, splash sequence) + celebrations (ThankYou/LevelUp), wipes filters/inputs; process death leaves §3.14 dead-end. Fix: `rememberSaveable` + `SavedStateHandle` + consume-once overlay flags.
- **Locale / font-scale chaos** — `Locale.US` (Home `SimpleDateFormat`/`format`/`toIso8601`, Gamification `"%.3fkg"`) vs default-locale `String.format("%.2f"/"%.1f%%"/"%.1f km")` (Dashboard/Locator) → mixed separators; month names fixed English; `category.uppercase()` w/o `Locale.ROOT` (Turkish-i); `lowercase()` w/o locale (Guide/KB); ~9–11sp secondary text + fixed-dp columns + `letterSpacing 2–12sp` clip at 1.3×+ scale (Home `ImpactStatCard 9sp maxLines=1`, Guide 10–11sp ×0.6 alpha, splash `12sp` + `Type.kt displaySmall 4sp`). Fix: one locale policy (`Locale.ROOT` for machine strings, user locale for display), scalable layouts, min text sizes.
- **Theming bypasses (aggregate HIGH)** — audit every `Color(` literal vs `Color.kt`/`Theme.kt`: Result/Barcode/YOLO black-on-primary; Gamification/ThankYou/LevelUp white-on-glass + `0xFF00FF94/0xFF2ECC71/0xFFFFD700`; Decay yellow `0xFFF1C40F` body (light fail) + red/green-only encoding (color-blind-unsafe, no pattern/label); Locator Navigate `0xFF00FF94/0xFF020408` + `TypeBadge` fixed palette (light-blue-on-light fail); History `trash→grey` vs Barcode `Trash→red` vs Guide `E-Waste→blue` palette drift (color semantics meaningless); `COLOUR` scheme missing container/variant/outline/error → Material3 violet/pink fallback; splash `Black` scrim + `White` title cuts into light/color Home; `ForestGreen`/`0xFFFFB300`/`HighConfidenceGreen` hard-codes. Fix: theme tokens only, single category→color map, color-blind-safe encodings.
- **A11y deficits** — meaningful icons `contentDescription=null` (Home Dataset/Visibility/MenuBook/chevrons/camera/scan; Result hazard/confidence; Feedback buttons "unlabeled button"); literal English descriptions (`"Settings"`, `"Retry"`, `"Toggle warning"`, `"Warning"`, `"Clear"`, `"Delete item"`, `"Tap anywhere"`, `"Thank You!"`, `"LEVEL UP!"`, `"MATERIAL BREAKDOWN"`, `"PACKAGING BREAKDOWN"`, `"TIME TO DECOMPOSE ⏳"` emoji read aloud, `"➔"`); undersized targets (YOLO/Guide inner-row clickables, YOLO 16dp dismiss, History 16dp glyphs ×0.5 alpha); rows/cards/overlays `.clickable` w/o `Role.Button/RadioButton`, no `selectableGroup`, no `expanded`/`heading`/`liveRegion` semantics; progress bars no semantics; timed auto-dismiss (ThankYou ~3.3s, LevelUp 4s) with full-overlay click, no visible button, no pause-on-touch (WCAG timed-content + TalkBack failure); `GradientActionButton` gradient-inside-content hides ripple + `LongPress` haptic for tap + ignores Settings haptics toggle; Settings slider hammers DataStore per-pixel (no `onValueChangeFinished` debounce) + threshold truncation `(t*100).toInt()`; Settings radio double-fire; flash-of-defaults (DARK/0.5 before prefs; LIGHT-on-dark first frame; `dynamicColor` flip); rotation-dismissed dialogs; splash forced ~4.1s, undiscoverable skip (whole-Box click, no Role/label). Fix: descriptions/roles/semantics/button-dismiss/min-48dp/ripple/single-haptic-source/debounce/Saveable/skip-control.
- **Loading/empty/error parity** — history hides section on first-frame empty (no skeleton vs "scan first" CTA); `Resolving`/`result-null` infinite spinners (no timeout); `centers.isEmpty()` silent; model-init failure discovered only on Classify press; CSV in-memory build (OOM), formula-injection unsanitized (`=,+,-,@`), missing columns (`source/productName/barcode/imagePath`), log-only failure, no success confirm; share sheet hard-coded English + emoji, hazard keyed on misspellable subclass. Fix: per-state UI + streaming export + sanitize + confirm + `WasteMapping`-based hazard.
- **Perf/battery** — `GlassCard blur(16.dp)` per card + `OrganicBackground` 5 infinite anims ×4 screens + `AppLogo` 4 anims (only 1 gated) + `SinEasing` stuck ~0.5–0.82 (muted pulse); `FallingPetals` only power-save-gated (add reduced-motion + off-screen + Settings toggle; also ignores own `modifier`, 30s restart teleports); LevelUp `while(true) withFrameMillis` mutation-in-draw + px Schwarze confetti (density-ignorant); ThankYou particle mutation-in-draw + glow-gated frames + irreversible alpha; Dashboard `360−n×3°` single-slice gap + full-width gaps during sweep + `Float` precision + redundant `FlowRow` wrapper + stale `estimatedWeight ?: 0.0` (non-null); `FallingPetals` 60/120fps idle drain; camera JPEGs leak (`cacheDir/camera_capture_*.jpg` never purged); worker full-table load (see §3.38). Fix: gate/reduce/share animations, dp-based particles, inset donut (`halfStroke`), purge cache, targeted queries.
- **Guide specifics** — literal chips (same taxonomy bug), wrong empty string (`history_no_search_results` for encyclopedia), `items(key=subclass)` duplicate-key crash risk (`MAPPING`+`EXTENDED` overlap), raw `snake_case` titles, nested clickable double-actions, KB load on UI thread in `remember` (needs dispatcher), search omits benefits/sources/weight.
- **Settings specifics** — blank screen on bad `Application` cast (`?: return`, no error); purge success-only snackbar; license bodies literal + unverified `"NOTICE and docs/ATTRIBUTION.md"` claim; `"DETECTION & ACCURACY"` literal; no reset-to-defaults; `setConfidenceThreshold` no `coerceIn(0.20,0.90)`; DataStore errors unhandled.
- **Splash specifics** — fixed chain (200dp logo + `-140dp` offset + 100/120/80dp spacers) clips compact/landscape; non-scroll `Column`; rotation restarts (`stage/logoOffset/displayedTitle` non-Saveable); random non-localized thought; infinite `CircularRsAnimation` for splash-only.
- **Barcode specifics** — permission auto-fire w/o rationale, permanent-denial loop (deep-link Settings instead); titles/buttons hard-coded (`"Barcode Scanner"`, `"Manual Barcode Entry"`, `"Resolving Product…"`, `"View Disposal Guide"` mislabeled — it saves — `"Scan Another"`, `"Analyze with Camera"`, `"PACKAGING BREAKDOWN"`, `"Weight: ${weight}g"` raw doubles, `NetworkStatusBadge` literals); rotation loses form; >20-char QR truncated in header but 512-char unbounded in body; Quick Classifier `selectedIndex=0` pre-selects E-Waste (one-tap cardboard→e-waste), blank names save labels as `productName`, `"General Trash"/Plastic` contradictory, all hard-coded + fixed accents; back should collapse sheet first (`resumeScanning`); focus ring (1200ms) vs AF cancel (3s) mismatch.
- **Result specifics** — `fillMaxSize()` inside `verticalScroll` (unbounded constraints warning); raw `plastic_bottle` labels + `FontFamily.Monospace`; `record != null` "Metadata only" always-true confusion; back dead-ends into §3.14 empty Classify.

---

## 5. LOW / nits / drift (fix opportunistically)

`totalXp` fake-0 fallback; `Locale.US` hard-wire; `toRelativeTime` English; `Locale.ROOT` missing ×3; `EmeraldVibrant` shadows theme; `MintAccent=SageGreen` alias; `Type.kt` spacing; `Theme.kt COLOUR` glass reuse; `WasegMulApp` no StrictMode/tracing; `WasteDatabase` `industrial` name drift, `exportSchema` w/o verified `schemas/`, no test `close()` hook; `Mappers` timestamp heal masks bug; `BarcodeProduct` clock-defaults nondeterminism, no TTL/version; `DisposalModels` lat/lon unvalidated, `acceptedCategories` case-sensitive; `ModelManager` dead branches, unused `TfLite` import, `"Unknown"/0f` abstain contract; `WM`/`TAG` abbreviations; `feedback`/`correction`/`code`/`type` param names (→ `correctedSubclass`/`barcode`/`category`); `shape="Item"/"Packaging"` literals; `VIBRATE` possibly unused; `READ_MEDIA_IMAGES` lint scoping; `local.properties` absolute path (ignored — expected); proguard duplicate keeps; `BarcodeScannerTest` eager-init fragility; `EcoImpactTest:128` misplaced KB assert; `taxonomy.yaml:4` "52+3" vs ~50+3, `Plastic glooves` typo+dup, `detection_to_subclass` paper/cup overlaps undocumented, `datasets_manifest custom.classes: 30` (EfficientNet count on YOLO labels); `taxonomy.md` counts hand-written; `DisposalDatabaseTest/Gamification/Mappers/Repo/ArbitratorBarcode` deterministic — keep.

---

## 6. Domain-language violations (vs CONTEXT.md — sweep list)

Rename: `type→category` (`HomeScreen RecentItem:691-693,464`), `subcategory→subclass` (all `CommonModels:40-41,49-51,63,72`, `MessageGenerator`, `IOSBridge`, `ClassificationViewModel.correction`), `code→barcode` (`isValidBarcode`), `GreenScore/EcoPoints/CarbonReduction→Eco Impact`, `ModelResolver/DecisionTree/ClassifierDecider→ML Arbitrator`, `ObjectFinder/BoxLocator→YOLO Detector`, `DisposalWiki/GuidanceStore/RuleEngine→Knowledge Base`, `ScannedItem/BarcodeEntry/ProductRow→Barcode Product`, `PackagingPart/ContainerPiece→Packaging Component`, `BarcodeSearch/CodeLookup→Barcode Resolution`, `ScanItem/LogEntry/TrashRow→Waste Record`, `NEURAL SCANS/neural history/SCAN INTERFACE/Quick Product Classifier/Unindexed Code/ScannedItem→Waste Record/Classification/ML Arbitrator/Barcode Resolution/Barcode Product`; `PACKAGING BREAKDOWN→Packaging Component breakdown`; `MATERIAL BREAKDOWN/CARBON OFFSET→Category/Eco Impact`; stop surfacing `featureVector` pipe-tags + `Storage: Image/Metadata`; never put Categories in `correctedSubclass` (`material_options`); reconcile `Trash↔Residual`, `Hazardous`-flag↔category; `wasegmul_industrial_v1.db` → neutral name.

---

## 7. Misinformation / wrongness catalog (user-visible or analytics-visible)

1. Bengaluru "Nearest" for all users (§3.15). 2. `0 YEARS` flash for weeks/months items (§3.55). 3. "High-confidence … highly dependable" at 0.55 (§3.52). 4. "Industrial/chemical precise confidence" (§3.24). 5. Styrofoam 50y (→500+y), cigarette fallback 50–1000y (→10–12y), `plastic_bag` 50–500y (→10–20y), season/generation buckets (§3.55). 6. Inverted mining stat (§3.54). 7. Mercury flat-screens, missing Li-ion fire handling (§3.54). 8. PVC/PS ↔ KB Plastic contradiction; Tetra Pak/bag/PLA overstated; wine-glass recyclable (§3.53/56). 9. Fabricated barcode weights → fake CO₂/water/energy (§3.29). 10. Coarse Eco factors presented as EPA-precise; E-waste CO₂ low; Hazardous invented (§3.51). 11. "5 NEURAL SCANS" cap (prior; verify current DAO limit). 12. Helplines/WhatsApp unverified + single-city shown globally (§2.4 + §3 Civic). 13. License `"NOTICE and docs/ATTRIBUTION.md"` unverified. 14. README: `0.0–1.0` vs raw `[0,255]` (§3.33), NMS `0.35` vs `0.45` (`YoloDetector:443`), Room indices vs CHANGELOG vs code, "8 suites + ExampleUnitTest" vs 13+3, "<50ms" unverified, `1.1.0/2` vs `2.0.0/3` vs iOS `V1.1.0` version drift. 15. Donut/scale visuals minimizing plastic (§3 Decay scale bar: 80y→0.3, 4500y→0.7 breaks proportionality).

---

## 8. Studio-class visual + usability checklist (what "done" looks like)

- [ ] Single category→color map, theme tokens only, color-blind-safe + label/pattern encodings, light/dark/colour contrast AA verified per screen.
- [ ] 48dp targets, visible button-dismiss (no timed-only overlays), full semantics (roles, headings, liveRegions, expanded, progress), meaningful descriptions, localized strings only (no emoji-as-content, no hard-coded English).
- [ ] Loading vs empty vs error on every async surface (history, resolving, result-null, model-init, locator, export, purge) with timeouts + retry + success confirms.
- [ ] Rotation/Saveable/process-death safe (filters, forms, bitmaps via `imagePath` reload, overlay consume-once flags, no replay celebrations/entrances).
- [ ] One locale policy + scalable type/layouts (no fixed-column overflow, no 9sp essential text, `Locale.ROOT` for machine strings).
- [ ] Gated/reduced animations (power-save + reduced-motion + off-screen + Settings toggle), no per-card blur tax, dp-based particles, no mutation-in-draw.
- [ ] Truthful copy + cited science (decay/factors/helplines/licenses) + city-aware civic content + disclosed estimates (weights, default location).
- [ ] iOS parity plan or explicit non-parity disclosure (no "coming soon" dead buttons in production).

---

## 9. Security / privacy notes

No SQL injection (bound params; seed projection hardcoded — safe). Exported: launcher `MainActivity` (`exported=true` correct) + `FileProvider` (`exported=false`, cache-only — good). HTTPS OFF + timeouts + UA — good. `allowBackup=false` + backup/extraction rules + `cleartext=false` — good. `FLAG_IMMUTABLE`, notification permission gating, no PII in notification body — good. Remaining: encrypt Room (SQLCipher) + image-file handling + TTL; fix worker retry-forever; rotate keystore creds to CI secrets; add startup/StrictMode observability; justify/remove `VIBRATE`; verify location/notification runtime-grant paths (declared correctly).

---

## 10. What was spot-checked by the lead auditor (evidence, not hearsay)

- `AndroidManifest.xml:1-73` — INTERNET + ACCESS_NETWORK_STATE + `<queries>` (maps/WhatsApp/geo/https/tel) **present**; FileProvider scoped; backup rules set. Old-report §2.1/§2.5 **fixed**.
- `EcoImpactCalculator.kt:21-38,91-117` — factors **now match** `architecture.md` (1.8/0.5/3.5/2.2, 25/2/15/10, 4.2/0.3/6.5/3.0). Old-report §3.3 numbers **fixed**; coarseness/citation caveat remains (§3.51).
- `ResultScreen.kt:63-64,70-92` — overlay `remember(isFreshScan/isFreshScan)` resurrection + Bengaluru fallback + never-request confirmed.
- `MLArbitrator.kt:32-59` — tunable `arbitrate(prediction, lowThreshold)` entry exists (contradicts old "placebo" claim for Arbitrator); `0.65f` fixed bar + `1.2f` ceiling confirmed (remaining contradiction = §3.50).
- `ImagePreprocessor.kt:23-25,32-36` — Resize-only, no `NormalizeOp`, HARDWARE null-risk confirmed; training-vs-README truth still open (§3.33).

---

## 11. Prioritized roadmap

### Phase 1 — Blockers (no release without these)
1. YOLO Main-thread violations (§2.1) + close race (§2.8) + inference dispatcher (§3.25); verify camera unbind.
2. Intent guards (§2.3–2.4) + light-mode text (§2.2/2.5) + single category-color map (§4).
3. OOM/wipe guards: shared `ModelManager` (§2.6), remove destructive fallback (§2.7), paginate export + worker queries (§3.38/3.45).
4. Truth fixes: preprocessor normalization (§3.33), INT8 calibration-or-remove + always emit labels (§3.47), `normalize_ean13` + curated barcodes (§3.47), taxonomy unification (§2.10/§3.47).
5. Barcode correctness: IO dispatcher + sanitize + word-boundary matching + fan-out/timeout/cancellation (§3.32), no poison/fake/estimates (§3.29), XP wiring + save-deadlock + cancel (§3.26–3.28, §3.30).
6. Location honesty (§3.15) + civic guards/contacts (§2.4) + secrets rotation (§2.9).

### Phase 2 — Logic / analytics / domain accuracy
7. Arbitrator zero/preserve + threshold + entropy + NaN + taxonomy validation (§3.50); Codec guards (§3.46); CommonModels policy/serial/enums (§3.59).
8. Mapping/KB/Decay/Packaging science pass (§3.53–3.56) + Eco counting/naming/citations/bridge (§3.51) + Message pools/RNG/i18n (§3.52) + Bridge parity/docs (§3.57) + OFF conventions/leniency/path/ownership (§3.58).
9. Correction taxonomy validation + weight sync (verify current `WasteRepository.updateCorrectionWithCategory` — old report flagged orphaned weight; re-verify + log unmapped) + nav policies + dead-tap wiring (§3.11–3.12) + error snackbars (§3.21) + loading-vs-empty (§3.20).

### Phase 3 — Studio polish / perf / parity
10. A11y/i18n/theming sweep (§4) + Saveable/process-death (§4) + animation gating + donut inset + cache purge (§4).
11. Tests: prod-helper exposure + YOLO/preprocess/classifier/migration/VM/worker/OFF suites + matrix update (§3.49); pinned `scripts/requirements-train.txt`, pHash dedup, stratified-split enforcement, cache opt-in, epoch/quota handling (§3.47).
12. Docs single-source: CONTEXT categories, README numbers/versions/matrix, taxonomy counts generated, `detection_to_subclass` rationale, CHANGELOG/indices agreement (§5/§7.14).
13. iOS parity roadmap or explicit scope cut (§3.48).

---

## 12. Relation to v1 report (supersedes but preserves)

**Fixed since v1 (verified above, do not re-file as-is):** manifest INTERNET/ACCESS_NETWORK_STATE + `<queries>`; Eco factor numbers; (verify before closing) Home counter cap, gamification cap, donut inset, confidence-threshold placebo for Arbitrator (now tunable entry — remaining gap is YOLO threshold + `0.65f` contradiction), YOLO COCO guard (`YoloDetector:118-135` forces `COCO_LABELS` on 80-class — keep + rename script output so the guard isn't load-bearing).
**Still open from v1:** COCO-as-waste-model pipeline confusion, codec delimiter risk (re-verify current `BarcodeScanViewModel` join vs `PredictionCodec.encode`), ProGuard DTO/enum keeps (re-verify current `proguard-rules.pro` vs v1 claim), Organic `sub==cat` fallback shape (re-verify vs current `WasteKnowledgeBase:30` + case bug §3.54), correction-weight sync (re-verify), barcode XP wiring (still open §3.26), cache/JPEG + petal battery (still open §4), worker full-table load (re-verify current `DailyImpactWorker` — infra audit says targeted `getCount/getRecordsSince` good, v1 says full load — reconcile by reading current file), iOS parity (still open).

---

*End of deep-dive audit. Generated from 4 parallel file-by-file passes + lead spot-checks.*

---

## 13. Re-audit verification — v3.0.0 production release (2026-09-19)

Every numbered finding was re-verified against the shipped tree; the loop below is the
evidence (device: real `e7bd80fe` + `Pixel_6` emulator, `com.agrelius.wasegmul.debug`):

- **Result entry kill (user-reported, post-audit):** reproduced on-device —
  `IllegalArgumentException: ending radius must be > 0` at `ThankYouOverlay.kt:160`
  (glow animation starts at radius 0). Fixed with `shouldDrawCelebrationGlow` gate +
  `CelebrationGlowTest` (4 tests). Re-driven YOLO → capture → classify (**real
  inference, +130 XP**) → celebration three times across two devices: PID alive,
  zero FATAL. The "no inference works at all" perception was this crash hiding
  working inference.
- **Crash batch (§2):** full-frame OOM contained at both handoff (`downscaleForHandoff`)
  and VM bound; barcode fire-and-forget + provider-bind + stale-Job + frame-leak fixed;
  13 new tests. `wasegmul://barcode` warm-start fixed (`onNewIntent` forwarding).
- **UI batch (§4):** equal stat cards, high-contrast CTAs (verified dark + light),
  visible petals/background, complete M3 palettes, level/XP card, YOLO guidance cards +
  smoother (600 ms / −0.10 / 0.02), 12 new tests. Follow-up polish in v3: pill
  FlowRow, XP/CO₂ wrap fixes — all screenshot-verified.
- **Shared batch (§3.50–59):** 10 new suites, 100/100 green; `Residual` alias handling
  extended to EcoImpact in v3 with test.
- **Data batch (§3.32–46):** repository rewrite, XP gates, worker retry policy,
  desugaring enabled; 115/115 at the time, 162/162 app tests at release.
- **Scripts batch (§3.47–49):** pinned requirements, calibration gate, checksum-gated
  barcode DB, taxonomy single-source; `py_compile` clean.
- **Release totals:** app 23 suites / 162 tests green, shared 13 suites / 100 green,
  `assembleDebug` clean, install Success, `USER_AGENT`/`VERSION_NAME`/`kAppVersionString`
  synced at 3.0.0.

Known non-blocking follow-ups: per-material LCA tables, Robolectric YOLO-parse/Room
migration tests, OFF v3 live integration test, full iOS parity (out of scope), field
tuning of smoother constants, real-device lit-scene detection grading.
